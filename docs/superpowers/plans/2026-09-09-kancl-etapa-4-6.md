# Kancl · etapy 4–6 — implementační plán (provedeno 2026-09-09 přes noc)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Etapy 4 (sezení), 5 (zdraví) a 6 (přehled) z artefaktu „Kancl: co dál", každá samostatně nasaditelná a commitnutá.

**Architecture:** Nové čisté moduly s testy (`desktop.ts` parser + index, `history.ts` filtr/rotace/digest, `projects.ts` skupiny), rozšíření skeneru (`gh run list`, mtime změn, `merge-base`), klient: názvy sezení, deep link, fronta, Tab, `?mini=1`, `?digest=1`, hledání, skupiny, připnutí.

**Tech Stack:** beze změny.

Spec: `docs/superpowers/specs/2026-09-09-kancl-etapa-4-6-design.md`.

---

### Task 1 · Etapa 4 (commit `0b49e9d`)
- [x] `server/desktop.ts` + `tests/desktop.test.ts` (parser local_*.json, index s mtime cache, novější aktivita vyhrává)
- [x] `Session.title`, `Session.desktopId`; `Store.desktopResolver`, `refreshDesktop()`
- [x] `focusTerminal(t, desktopId)` → `open claude://code/continue?session=…` (regex z bundlu `^local_[A-Za-z0-9-]{1,64}$`)
- [x] Panel: název sezení jako hlavní jméno, přezdívka malá; řazení fronty podle `statusSince`; souhrn „nejdéle čeká"
- [x] `Tab` / `Shift+Tab` cyklí frontu „chce mě"
- [x] `client/src/ui/mini.ts`, `?mini=1`, styly
- [x] Ověřeno: reálné sezení má název z aplikace a `desktopId`; mini režim v 360×260

### Task 2 · Etapa 5 (commit `06f0f1c`)
- [x] `Project.ci` (`gh run list --limit 3`), `Worktree.dirtyOldest/merged/stale`
- [x] `server/history.ts` + `tests/history.test.ts`; `Store.onSessionEnd`; `/api/history`, `/api/digest`
- [x] `attention.setNight()`: chyba úlohy, starý snímek, alarm zásoby
- [x] Panel: `CI ✗`, „nejstarší N d", štítek `zastaralé` + kopírování `git worktree remove`
- [x] Ověřeno na reálných datech: CI selhání deky/Dopner/baliky/kayla-rebuild/apartina, zastaralé `katalogodpadu-cite`, historie 6 událostí

### Task 3 · Etapa 6 (commity `b1e5c30`, `+fix`)
- [x] `assignGroups`/`matchGlob` + `tests/groups.test.ts`; config `groups` s výchozími Shean / Klienti / Vlastní weby
- [x] Panel: hledání (`/`, Esc), záhlaví skupin (připnuté → v práci → skupiny z configu → ostatní), špendlík, localStorage pro filtr/rozbalení/zoom/připnutí
- [x] `client/src/ui/digest.ts`, `?digest=1`; README (sezení z aplikace, zdraví, skupiny, Tailscale)
- [x] Ověřeno: 3 šířky bez přetečení, digest na reálných datech

## Neuděláno (potřebuje Davida)
- `GITLAB_TOKEN`, `CRONJOB_API_KEY` do `~/Code/kancl/.env`
- Tailscale: jen dokumentace (`KANCL_HOST`)
- Deep link `claude://code/continue` byl otestován jen tím, že aplikace odkaz přijala; přepnutí okna je potřeba vidět naživo
